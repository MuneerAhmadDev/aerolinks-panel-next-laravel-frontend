import ExpenseLedger from "@/components/Finance/ExpenseLedger";

export const metadata = { title: "Expense Ledger — Finance" };

export default function ExpensesPage() {
  return <ExpenseLedger />;
}
