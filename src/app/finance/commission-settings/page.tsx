import CommissionSettings from "@/components/Finance/CommissionSettings";

export const metadata = { title: "Commission Slabs — Finance" };

export default function CommissionSettingsPage() {
  return <CommissionSettings />;
}
