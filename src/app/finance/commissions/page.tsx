import CommissionsLedger from "@/components/Finance/CommissionsLedger";

export const metadata = { title: "Commissions — Finance" };

export default function CommissionsPage() {
  return <CommissionsLedger />;
}
